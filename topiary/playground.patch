--- a/Cargo.toml
+++ b/Cargo.toml
@@ -25,6 +25,7 @@ crate-type = ["cdylib"]
 [dependencies]
 topiary-core = "0.5.1"
 topiary-config = "0.5.1"
+topiary-web-tree-sitter-sys = "0.5.1"
 topiary-tree-sitter-facade = "0.5.1"
 wasm-bindgen = "=0.2.91"
 wasm-bindgen-futures = "=0.4"
--- a/web-playground/src/App.tsx
+++ b/web-playground/src/App.tsx
@@ -15,4 +15,4 @@ const debounceDelay = 500;
 function App() {
-    const defaultLanguage = "json";
+    const defaultLanguage = "magma";
     const defaultQuery = languages[defaultLanguage].query;
     const defaultInput = languages[defaultLanguage].input;
@@ -187,4 +187,4 @@
-                <select id="languageMenu" onChange={e => changeLanguage(e.target.value)}>
+                <select id="languageMenu" value={currentLanguage} onChange={e => changeLanguage(e.target.value)}>
                     <option value="">Choose a reference language</option>
                     {languageOptions}
                 </select>
--- a/src/lib.rs
+++ b/src/lib.rs
@@ -36,6 +36,31 @@ mod wasm_mod {
     #[wasm_bindgen(js_name = queryInit)]
     pub async fn query_init(query_content: String, language_name: String) -> Result<(), JsError> {
         let language_normalized = language_name.replace('-', "_");
+
+        if language_normalized == "magma" {
+            let grammar: topiary_tree_sitter_facade::Language =
+                topiary_web_tree_sitter_sys::Language::load_path(
+                    "scripts/tree-sitter-magma.wasm",
+                )
+                .await
+                .map_err(|e| -> JsError {
+                    let err: topiary_tree_sitter_facade::LanguageError = e.into();
+                    err.into()
+                })?
+                .into();
+            let query = TopiaryQuery::new(&grammar, &query_content)?;
+            let mut guard = QUERY_STATE.lock().unwrap();
+            *guard = Some(QueryState {
+                language: Language {
+                    name: "magma".to_string(),
+                    query,
+                    grammar,
+                    indent: Some("    ".to_string()),
+                },
+            });
+            return Ok(());
+        }
+
         let configuration = Configuration::default();
         let language = configuration.get_language(language_normalized)?.clone();
         let grammar = language.grammar().await?;
